// entities
interface Org {
  id: string;
  name: string;
  description: string;
  created: string;
  updated: string;
}

interface User {
  id: string;
  name: string;
  email: string;
  created: string;
  updated: string;
}

interface Suite {
  id: string;
  name: string;
  description: string;
  created: string;
  updated: string;
}

interface Test {
  id: string;
  name: string;
  description: string;
  created: string;
  updated: string;
}
